import { invariant, ToNumber } from '@formatjs/ecma402-abstract';
export function ToIntegerIfIntegral(arg) {
    var number = ToNumber(arg);
    invariant(number.isInteger(), "".concat(arg, " is not an integer"));
    return number.toNumber();
}
