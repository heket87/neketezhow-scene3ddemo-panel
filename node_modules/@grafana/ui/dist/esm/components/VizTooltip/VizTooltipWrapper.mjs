import { jsx } from 'react/jsx-runtime';
import { css } from '@emotion/css';
import clsx from 'clsx';
import { selectors } from '@grafana/e2e-selectors';
import { useStyles2 } from '../../themes/ThemeContext.mjs';

"use strict";
const VizTooltipWrapper = ({ children, className }) => {
  const styles = useStyles2(getStyles);
  return /* @__PURE__ */ jsx("div", { className: clsx(styles, className), "data-testid": selectors.components.Panels.Visualization.Tooltip.Wrapper, children });
};
const getStyles = (theme) => css({
  display: "flex",
  flexDirection: "column",
  fontSize: theme.typography.bodySmall.fontSize
});

export { VizTooltipWrapper };
//# sourceMappingURL=VizTooltipWrapper.mjs.map
