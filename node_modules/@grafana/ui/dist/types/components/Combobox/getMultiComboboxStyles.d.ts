import { GrafanaTheme2 } from '@grafana/data';
export declare const getMultiComboboxStyles: (theme: GrafanaTheme2, isOpen: boolean, invalid?: boolean, disabled?: boolean, width?: number | "auto", minWidth?: number, maxWidth?: number, isClearable?: boolean) => {
    container: string;
    wrapper: string;
    input: string;
    pillWrapper: string;
    restNumber: string;
    suffix: string;
    disabled: string;
};
