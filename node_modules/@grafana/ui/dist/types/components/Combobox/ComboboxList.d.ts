import type { UseComboboxPropGetters } from 'downshift';
import { ComboboxOption } from './types';
export declare const VIRTUAL_OVERSCAN_ITEMS = 4;
interface ComboboxListProps<T extends string | number> {
    options: Array<ComboboxOption<T>>;
    highlightedIndex: number | null;
    selectedItems?: Array<ComboboxOption<T>>;
    scrollRef: React.RefObject<HTMLDivElement | null>;
    getItemProps: UseComboboxPropGetters<ComboboxOption<T>>['getItemProps'];
    enableAllOption?: boolean;
    isMultiSelect?: boolean;
    error?: boolean;
    loading?: boolean;
}
export declare const ComboboxList: <T extends string | number>({ options, highlightedIndex, selectedItems, scrollRef, getItemProps, enableAllOption, isMultiSelect, error, loading, }: ComboboxListProps<T>) => import("react/jsx-runtime").JSX.Element;
export {};
