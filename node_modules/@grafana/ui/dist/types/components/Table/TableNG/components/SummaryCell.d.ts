import { GrafanaTheme2, Field } from '@grafana/data';
import { TableFooterOptions } from '@grafana/schema';
import { TableRow } from '../types';
import { TextAlign } from '../utils';
interface SummaryCellProps {
    rows: TableRow[];
    field: Field;
    footers: Array<TableFooterOptions | undefined>;
    textAlign: TextAlign;
    colIdx: number;
    rowLabel?: boolean;
    hideLabel?: boolean;
}
export declare const SummaryCell: ({ rows, footers, field, colIdx, hideLabel, rowLabel, textAlign, }: SummaryCellProps) => import("react/jsx-runtime").JSX.Element;
export declare const getStyles: (theme: GrafanaTheme2, textAlign: TextAlign, hideLabel: boolean) => {
    footerCell: string;
    footerItem: string;
    footerItemLabel: string;
    footerItemValue: string;
};
export {};
