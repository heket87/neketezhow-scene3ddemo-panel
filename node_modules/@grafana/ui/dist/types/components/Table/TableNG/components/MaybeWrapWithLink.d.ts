import { ReactNode } from 'react';
import { Field } from '@grafana/data';
interface MaybeWrapWithLinkProps {
    field: Field;
    rowIdx: number;
    children: ReactNode;
}
export declare const MaybeWrapWithLink: import("react").MemoExoticComponent<({ field, rowIdx, children }: MaybeWrapWithLinkProps) => ReactNode>;
export {};
