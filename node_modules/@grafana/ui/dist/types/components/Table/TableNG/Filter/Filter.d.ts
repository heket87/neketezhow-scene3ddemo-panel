import { Field } from '@grafana/data';
import { FilterType, TableRow } from '../types';
interface Props {
    name: string;
    rows: TableRow[];
    filter: FilterType;
    setFilter: React.Dispatch<React.SetStateAction<FilterType>>;
    field?: Field;
    crossFilterOrder: string[];
    crossFilterRows: {
        [key: string]: TableRow[];
    };
    iconClassName?: string;
}
export declare const Filter: import("react").MemoExoticComponent<({ name, rows, filter, setFilter, field, crossFilterOrder, crossFilterRows, iconClassName }: Props) => import("react/jsx-runtime").JSX.Element>;
export {};
