import { CSSProperties, ReactElement, RefObject } from 'react';
import { DataGridHandle } from 'react-data-grid';
import { ActionModel, DataFrame, Field, GrafanaTheme2 } from '@grafana/data';
import { TableCellTooltipPlacement } from '@grafana/schema';
import { TableCellOptions } from '../../types';
import { getTooltipStyles } from '../styles';
import { TableCellRenderer } from '../types';
export interface TableCellTooltipProps {
    cellOptions: TableCellOptions;
    children: ReactElement;
    classes: ReturnType<typeof getTooltipStyles>;
    className?: string;
    data: DataFrame;
    disableSanitizeHtml?: boolean;
    field: Field;
    getActions: (field: Field, rowIdx: number) => ActionModel[];
    getTextColorForBackground: (bgColor: string) => string;
    gridRef: RefObject<DataGridHandle | null>;
    height: number;
    placement?: TableCellTooltipPlacement;
    renderer: TableCellRenderer;
    rowIdx: number;
    style?: CSSProperties;
    tooltipField: Field;
    theme: GrafanaTheme2;
    width?: number;
}
export declare const TableCellTooltip: import("react").MemoExoticComponent<({ cellOptions, children, classes, className, data, disableSanitizeHtml, field, getActions, getTextColorForBackground, gridRef, height, placement, renderer: CellRenderer, rowIdx, style, theme, tooltipField, width, }: TableCellTooltipProps) => import("react/jsx-runtime").JSX.Element>;
