import { ReactNode } from 'react';
import { ActionModel, LinkModel } from '@grafana/data';
import { DataLinksActionsTooltipCoords } from './utils';
interface Props {
    links: LinkModel[];
    actions?: ActionModel[];
    value?: ReactNode;
    coords: DataLinksActionsTooltipCoords;
    onTooltipClose?: () => void;
}
/**
 *
 * @internal
 */
export declare const DataLinksActionsTooltip: ({ links, actions, value, coords, onTooltipClose }: Props) => import("react/jsx-runtime").JSX.Element | null;
export declare const renderSingleLink: (link: LinkModel, children: ReactNode, className?: string) => ReactNode;
export {};
