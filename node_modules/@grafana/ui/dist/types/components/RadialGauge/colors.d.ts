import tinycolor from 'tinycolor2';
import { FieldDisplay, GrafanaTheme2 } from '@grafana/data';
import { GradientStop } from './types';
export declare function buildGradientColors(theme: GrafanaTheme2, fieldDisplay: FieldDisplay, baseColor?: string): GradientStop[];
/**
 * get the relevant gradient stops surrounding a given percentage. could be same stop if the
 * percent matches a stop exactly.
 *
 * @param sortedGradientStops - gradient stops sorted by percent
 * @param percent - percentage 0..1
 * @returns {[GradientStop, GradientStop]} - the two gradient stops surrounding the given percentage
 */
export declare function getGradientStopsForPercent(sortedGradientStops: GradientStop[], percent: number): [GradientStop, GradientStop];
/**
 * @alpha - perhaps this should go in colorManipulator.ts
 * Given color stops (each with a color and percentage 0..1) returns the color at a given percentage.
 * Uses tinycolor.mix for interpolation.
 * @params stops - array of color stops (percentages 0..1)
 * @params percent - percentage 0..1
 * @returns color at the given percentage
 */
export declare function colorAtGradientPercent(stops: GradientStop[], percent: number): tinycolor.Instance;
export declare function getBarEndcapColors(gradientStops: GradientStop[], percent?: number): [string, string];
export declare function getGradientCss(gradientStops: GradientStop[], startAngle: number, endAngle: number): string;
export declare const getGuideDotColor: (color: string) => string;
export declare function getEndpointMarkerColors(gradientStops: GradientStop[], percent?: number): [string, string];
