import { ReactElement } from 'react';
import * as React from 'react';
interface LabelProps {
    Component: ReactElement;
    onClick?: () => void;
    disabled?: boolean;
}
export declare const useExpandableLabel: (initialExpanded: boolean, onExpandedChange?: (expanded: boolean) => void) => [React.ComponentType<LabelProps>, number, boolean, (expanded: boolean) => void];
export {};
