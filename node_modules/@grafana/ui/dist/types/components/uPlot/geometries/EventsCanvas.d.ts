import { ReactNode } from 'react';
import { DataFrame } from '@grafana/data';
import { UPlotConfigBuilder } from '../config/UPlotConfigBuilder';
interface EventsCanvasProps {
    id: string;
    config: UPlotConfigBuilder;
    events: DataFrame[];
    renderEventMarker: (dataFrame: DataFrame, rowIndex: number) => ReactNode;
    mapEventToXYCoords: (dataFrame: DataFrame, rowIndex: number) => {
        x: number;
        y: number;
    } | undefined;
}
export declare function EventsCanvas({ id, events, renderEventMarker, mapEventToXYCoords, config }: EventsCanvasProps): import("react/jsx-runtime").JSX.Element | null;
export {};
