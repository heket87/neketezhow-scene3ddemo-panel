interface Props {
    name: string;
    disabled?: boolean;
    onRemove: (tag: string) => void;
    /** Colours the tags 'randomly' based on the name. Defaults to true */
    autoColors?: boolean;
}
/**
 * @internal
 * Only used internally by TagsInput
 * */
export declare const TagItem: ({ name, disabled, onRemove, autoColors }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
