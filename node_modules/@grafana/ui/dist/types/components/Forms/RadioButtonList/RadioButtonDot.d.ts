import * as React from 'react';
export interface RadioButtonDotProps<T> extends Omit<React.HTMLProps<HTMLInputElement>, 'label' | 'value' | 'onChange' | 'type'> {
    id: string;
    name: string;
    checked?: boolean;
    value?: T;
    disabled?: boolean;
    label: React.ReactNode;
    description?: string;
    onChange?: (id: string) => void;
}
export declare const RadioButtonDot: <T extends string | number | readonly string[]>({ id, name, label, checked, value, disabled, description, onChange, ...props }: RadioButtonDotProps<T>) => import("react/jsx-runtime").JSX.Element;
