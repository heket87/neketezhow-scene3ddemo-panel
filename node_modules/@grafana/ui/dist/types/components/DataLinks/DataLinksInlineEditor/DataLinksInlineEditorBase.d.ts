import { Action, DataFrame, DataLink } from '@grafana/data';
export interface DataLinksInlineEditorBaseProps<T extends DataLink | Action> {
    type: 'link' | 'action';
    items?: T[];
    onChange: (items: T[]) => void;
    data: DataFrame[];
    children: (item: T, index: number, onSave: (index: number, item: T) => void, onCancel: (index: number) => void) => React.ReactNode;
}
/** @internal */
export declare function DataLinksInlineEditorBase<T extends DataLink | Action>({ type, items, onChange, data, children, }: DataLinksInlineEditorBaseProps<T>): import("react/jsx-runtime").JSX.Element;
