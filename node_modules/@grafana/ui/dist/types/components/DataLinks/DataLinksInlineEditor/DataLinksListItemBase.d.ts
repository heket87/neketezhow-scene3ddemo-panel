import { Action, DataFrame, DataLink } from '@grafana/data';
export interface DataLinksListItemBaseProps<T extends DataLink | Action> {
    index: number;
    item: T;
    data: DataFrame[];
    onChange: (index: number, item: T) => void;
    onEdit: () => void;
    onRemove: () => void;
    isEditing?: boolean;
    itemKey: string;
}
/** @internal */
export declare function DataLinksListItemBase<T extends DataLink | Action>({ item, onEdit, onRemove, index, itemKey, }: DataLinksListItemBaseProps<T>): import("react/jsx-runtime").JSX.Element;
