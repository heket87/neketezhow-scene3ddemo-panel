import { DataFrame, DataLink, VariableSuggestion } from '@grafana/data';
interface DataLinkEditorModalContentProps {
    link: DataLink;
    index: number;
    data: DataFrame[];
    getSuggestions: () => VariableSuggestion[];
    onSave: (index: number, ink: DataLink) => void;
    onCancel: (index: number) => void;
    showOneClick?: boolean;
}
export declare const DataLinkEditorModalContent: ({ link, index, onSave, onCancel, getSuggestions, showOneClick, }: DataLinkEditorModalContentProps) => import("react/jsx-runtime").JSX.Element;
export {};
