import { TimeRange, TimeZone } from '@grafana/data';
import { WeekStart } from '../WeekStartPicker';
interface Props {
    isFullscreen: boolean;
    value: TimeRange;
    onApply: (range: TimeRange) => void;
    timeZone?: TimeZone;
    fiscalYearStartMonth?: number;
    roundup?: boolean;
    isReversed?: boolean;
    onError?: (error?: string) => void;
    weekStart?: WeekStart;
}
export declare const TimeRangeContent: (props: Props) => import("react/jsx-runtime").JSX.Element;
export {};
