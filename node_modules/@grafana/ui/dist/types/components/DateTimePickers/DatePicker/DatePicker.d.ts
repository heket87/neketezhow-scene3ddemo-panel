import { GrafanaTheme2 } from '@grafana/data';
/** @public */
export interface DatePickerProps {
    isOpen?: boolean;
    onClose: () => void;
    onChange: (value: Date) => void;
    value?: Date;
    minDate?: Date;
    maxDate?: Date;
}
/**
 * A component with a calendar view for selecting a date.
 *
 * https://developers.grafana.com/ui/latest/index.html?path=/docs/date-time-pickers-datepicker--docs
 * @public
 * */
export declare const DatePicker: import("react").NamedExoticComponent<DatePickerProps>;
export declare const getStyles: (theme: GrafanaTheme2) => {
    modal: string;
};
