import { TimeZoneInfo } from '@grafana/data';
interface Props {
    info?: TimeZoneInfo;
}
export declare const TimeZoneDescription: ({ info }: Props) => import("react/jsx-runtime").JSX.Element | null;
export {};
