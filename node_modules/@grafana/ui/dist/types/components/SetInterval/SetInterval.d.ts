import * as React from 'react';
import { SelectableValue } from '@grafana/data';
export declare function getIntervalFromString(strInterval: string): SelectableValue<number>;
interface Props {
    func: () => unknown;
    loading: boolean;
    interval: string;
}
/**
 * @deprecated
 */
export declare const SetInterval: React.MemoExoticComponent<({ func, loading, interval: intervalStr }: Props) => null>;
export {};
