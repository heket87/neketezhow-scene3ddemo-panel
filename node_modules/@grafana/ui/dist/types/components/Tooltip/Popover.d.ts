import * as React from 'react';
import { PopoverContent, TooltipPlacement } from './types';
interface Props extends Omit<React.HTMLAttributes<HTMLDivElement>, 'content'> {
    show: boolean;
    placement?: TooltipPlacement;
    content: PopoverContent;
    referenceElement: HTMLElement;
    wrapperClassName?: string;
    renderArrow?: boolean;
    hidePopper?: () => void;
    style?: React.CSSProperties;
}
export declare function Popover({ content, show, placement, className, wrapperClassName, referenceElement, renderArrow, hidePopper, style: styleOverrides, ...rest }: Props): import("react/jsx-runtime").JSX.Element | undefined;
export {};
