import { ChangeEvent, MouseEvent } from 'react';
interface Props {
    label: string;
    hasCert: boolean;
    placeholder: string;
    useGrow?: boolean;
    onChange: (event: ChangeEvent<HTMLTextAreaElement>) => void;
    onClick: (event: MouseEvent<HTMLButtonElement>) => void;
}
export declare const CertificationKey: ({ hasCert, label, onChange, onClick, placeholder, useGrow }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
