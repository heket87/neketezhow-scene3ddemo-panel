import { VizTooltipItem } from './types';
interface Props {
    item: VizTooltipItem;
    isPinned: boolean;
}
export declare const VizTooltipHeader: ({ item: { label, value, color, colorIndicator }, isPinned }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
