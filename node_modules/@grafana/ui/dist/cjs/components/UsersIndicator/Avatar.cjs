'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var jsxRuntime = require('react/jsx-runtime');
var css = require('@emotion/css');
var ThemeContext = require('../../themes/ThemeContext.cjs');
var responsiveness = require('../Layout/utils/responsiveness.cjs');

"use strict";
const Avatar = ({ src, alt, width, height }) => {
  const styles = ThemeContext.useStyles2(getStyles, width, height);
  return /* @__PURE__ */ jsxRuntime.jsx("img", { className: styles.image, src, alt });
};
const getStyles = (theme, width = 3, height = 3) => {
  return {
    image: css.css([
      responsiveness.getResponsiveStyle(theme, width, (val) => ({
        width: theme.spacing(val)
      })),
      responsiveness.getResponsiveStyle(theme, height, (val) => ({
        height: theme.spacing(val)
      })),
      { borderRadius: theme.shape.radius.circle }
    ])
  };
};

exports.Avatar = Avatar;
//# sourceMappingURL=Avatar.cjs.map
