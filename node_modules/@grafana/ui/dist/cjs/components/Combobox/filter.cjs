'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var data = require('@grafana/data');

"use strict";
function itemToString(item) {
  var _a;
  if (item == null) {
    return "";
  }
  return (_a = item.label) != null ? _a : item.value.toString();
}
function fuzzyFind(options, haystack, needle) {
  const indices = data.fuzzySearch(haystack, needle);
  return indices.map((idx) => options[idx]);
}

exports.fuzzyFind = fuzzyFind;
exports.itemToString = itemToString;
//# sourceMappingURL=filter.cjs.map
