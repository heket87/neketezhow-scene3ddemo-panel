import { PluginLoadingStrategy } from '@grafana/data';

"use strict";
function angularMapper(spec) {
  return {};
}
function loadingStrategyMapper(spec) {
  var _a, _b;
  const loadingStrategy = (_b = (_a = spec.module) == null ? void 0 : _a.loadingStrategy) != null ? _b : PluginLoadingStrategy.fetch;
  if (loadingStrategy === PluginLoadingStrategy.script) {
    return PluginLoadingStrategy.script;
  }
  return PluginLoadingStrategy.fetch;
}

export { angularMapper, loadingStrategyMapper };
//# sourceMappingURL=shared.mjs.map
