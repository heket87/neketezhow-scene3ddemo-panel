import { DataQuery, DataSourceApi, DataSourceInstanceSettings, TestDataSourceResponse } from '@grafana/data';
export declare abstract class RuntimeDataSource<TQuery extends DataQuery = DataQuery> extends DataSourceApi<TQuery> {
    instanceSettings: DataSourceInstanceSettings;
    constructor(pluginId: string, uid: string);
    testDatasource(): Promise<TestDataSourceResponse>;
}
