import { WithAccessControlMetadata } from '@grafana/data';
export declare const hasPermission: (action: string) => boolean;
export declare const hasPermissionInMetadata: (action: string, object: WithAccessControlMetadata) => boolean;
export declare const hasAllPermissions: (actions: string[]) => boolean;
export declare const hasAnyPermission: (actions: string[]) => boolean;
