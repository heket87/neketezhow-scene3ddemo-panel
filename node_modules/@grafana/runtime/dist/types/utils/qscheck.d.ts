import { DataSourceInstanceSettings, DataSourceJsonData } from '@grafana/data';
interface JsonData extends DataSourceJsonData {
    oauthPassThru?: unknown;
    azureCredentials?: {
        authType?: unknown;
    };
}
type DSSettings = DataSourceInstanceSettings<JsonData>;
export declare function isQueryServiceCompatible(datasources: DSSettings[], allowedTypes: unknown): boolean;
export {};
