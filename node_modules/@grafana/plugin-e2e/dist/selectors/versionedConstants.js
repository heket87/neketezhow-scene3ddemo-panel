'use strict';

var minGrafanaVersion = require('./minGrafanaVersion.js');

const versionedConstants = {
  PageToolBar: {
    itemButtonTitle: {
      "10.1.0": "Add button",
      [minGrafanaVersion.MIN_GRAFANA_VERSION]: "Add panel button"
    }
  },
  Select: {
    singleValueContainer: {
      [minGrafanaVersion.MIN_GRAFANA_VERSION]: () => 'div[class*="-grafana-select-value-container"] > div[class*="-singleValue"]'
    },
    multiValueContainer: {
      [minGrafanaVersion.MIN_GRAFANA_VERSION]: () => 'div[class*="-grafana-select-multi-value-container"] > div'
    }
  },
  Cascader: {
    menu: {
      [minGrafanaVersion.MIN_GRAFANA_VERSION]: () => 'div[class="rc-cascader-menus"]'
    }
  }
};

exports.versionedConstants = versionedConstants;
