import { SelectorsOf, VersionedSelectorGroup } from './types';
/**
 * Resolves selectors based on the Grafana version
 */
export declare function resolveSelectors<T extends VersionedSelectorGroup>(versionedSelectors: T, grafanaVersion?: string): SelectorsOf<T>;
