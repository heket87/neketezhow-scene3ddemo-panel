'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var d3Interpolate = require('d3-interpolate');
var d3ScaleChromatic = require('d3-scale-chromatic');
var stringHash = require('string-hash');
var tinycolor = require('tinycolor2');
var i18n = require('@grafana/i18n');
var colorManipulator = require('../themes/colorManipulator.cjs');
var fieldReducer = require('../transformations/fieldReducer.cjs');
var fieldColor = require('../types/fieldColor.cjs');
var Registry = require('../utils/Registry.cjs');
var scale = require('./scale.cjs');
var thresholds = require('./thresholds.cjs');

function _interopDefaultCompat (e) { return e && typeof e === 'object' && 'default' in e ? e : { default: e }; }

var stringHash__default = /*#__PURE__*/_interopDefaultCompat(stringHash);
var tinycolor__default = /*#__PURE__*/_interopDefaultCompat(tinycolor);

"use strict";
const fieldColorModeRegistry = new Registry.Registry(() => {
  const accessibleGroup = i18n.t("grafana-data.field.fieldColor.accessibleGroup", "Accessible");
  const otherGroup = i18n.t("grafana-data.field.fieldColor.otherGroup", "Others");
  return [
    {
      id: fieldColor.FieldColorModeId.Fixed,
      name: "Single color",
      description: "Set a specific color",
      getCalculator: getFixedColor
    },
    {
      id: fieldColor.FieldColorModeId.Shades,
      name: "Shades of a color",
      description: "Select shades of a specific color",
      getCalculator: getShadedColor
    },
    {
      id: fieldColor.FieldColorModeId.Thresholds,
      name: "From thresholds",
      isByValue: true,
      description: "Derive colors from thresholds",
      getCalculator: (_field, theme) => {
        return (_value, _percent, threshold) => {
          const thresholdSafe = threshold != null ? threshold : thresholds.fallBackThreshold;
          return theme.visualization.getColorByName(thresholdSafe.color);
        };
      }
    },
    new FieldColorSchemeMode({
      id: fieldColor.FieldColorModeId.PaletteClassic,
      name: "Classic palette",
      isContinuous: false,
      isByValue: false,
      getColors: (theme) => {
        return theme.visualization.palette;
      }
    }),
    new FieldColorSchemeMode({
      id: fieldColor.FieldColorModeId.PaletteClassicByName,
      name: "Classic palette (by series name)",
      isContinuous: false,
      isByValue: false,
      useSeriesName: true,
      getColors: (theme) => {
        return theme.visualization.palette.filter(
          (color) => colorManipulator.getContrastRatio(theme.visualization.getColorByName(color), theme.colors.background.primary) >= theme.colors.contrastThreshold
        );
      }
    }),
    new FieldColorSchemeMode({
      id: fieldColor.FieldColorModeId.ContinuousViridis,
      name: "Viridis",
      isContinuous: true,
      isByValue: true,
      interpolator: d3ScaleChromatic.interpolateViridis,
      group: accessibleGroup
    }),
    new FieldColorSchemeMode({
      id: fieldColor.FieldColorModeId.ContinuousMagma,
      name: "Magma",
      isContinuous: true,
      isByValue: true,
      interpolator: d3ScaleChromatic.interpolateMagma,
      group: accessibleGroup
    }),
    new FieldColorSchemeMode({
      id: fieldColor.FieldColorModeId.ContinuousPlasma,
      name: "Plasma",
      isContinuous: true,
      isByValue: true,
      interpolator: d3ScaleChromatic.interpolatePlasma,
      group: accessibleGroup
    }),
    new FieldColorSchemeMode({
      id: fieldColor.FieldColorModeId.ContinuousInferno,
      name: "Inferno",
      isContinuous: true,
      isByValue: true,
      interpolator: d3ScaleChromatic.interpolateInferno,
      group: accessibleGroup
    }),
    new FieldColorSchemeMode({
      id: fieldColor.FieldColorModeId.ContinuousCividis,
      name: "Cividis",
      isContinuous: true,
      isByValue: true,
      interpolator: d3ScaleChromatic.interpolateCividis,
      group: accessibleGroup
    }),
    new FieldColorSchemeMode({
      id: fieldColor.FieldColorModeId.ContinuousGrYlRd,
      name: "Green-Yellow-Red",
      isContinuous: true,
      isByValue: true,
      getColors: (theme) => ["green", "yellow", "red"],
      group: otherGroup
    }),
    new FieldColorSchemeMode({
      id: fieldColor.FieldColorModeId.ContinuousRdYlGr,
      name: "Red-Yellow-Green",
      isContinuous: true,
      isByValue: true,
      getColors: (theme) => ["red", "yellow", "green"],
      group: otherGroup
    }),
    new FieldColorSchemeMode({
      id: fieldColor.FieldColorModeId.ContinuousBlYlRd,
      name: "Blue-Yellow-Red",
      isContinuous: true,
      isByValue: true,
      getColors: (theme) => ["dark-blue", "super-light-yellow", "dark-red"],
      group: otherGroup
    }),
    new FieldColorSchemeMode({
      id: fieldColor.FieldColorModeId.ContinuousYlRd,
      name: "Yellow-Red",
      isContinuous: true,
      isByValue: true,
      getColors: (theme) => ["super-light-yellow", "dark-red"],
      group: otherGroup
    }),
    new FieldColorSchemeMode({
      id: fieldColor.FieldColorModeId.ContinuousBlPu,
      name: "Blue-Purple",
      isContinuous: true,
      isByValue: true,
      getColors: (theme) => ["blue", "purple"],
      group: otherGroup
    }),
    new FieldColorSchemeMode({
      id: fieldColor.FieldColorModeId.ContinuousYlBl,
      name: "Yellow-Blue",
      isContinuous: true,
      isByValue: true,
      getColors: (theme) => ["super-light-yellow", "dark-blue"],
      group: otherGroup
    }),
    new FieldColorSchemeMode({
      id: fieldColor.FieldColorModeId.ContinuousBlues,
      name: "Blues",
      isContinuous: true,
      isByValue: true,
      getColors: (theme) => ["panel-bg", "dark-blue"],
      group: otherGroup
    }),
    new FieldColorSchemeMode({
      id: fieldColor.FieldColorModeId.ContinuousReds,
      name: "Reds",
      isContinuous: true,
      isByValue: true,
      getColors: (theme) => ["panel-bg", "dark-red"],
      group: otherGroup
    }),
    new FieldColorSchemeMode({
      id: fieldColor.FieldColorModeId.ContinuousGreens,
      name: "Greens",
      isContinuous: true,
      isByValue: true,
      getColors: (theme) => ["panel-bg", "dark-green"],
      group: otherGroup
    }),
    new FieldColorSchemeMode({
      id: fieldColor.FieldColorModeId.ContinuousPurples,
      name: "Purples",
      isContinuous: true,
      isByValue: true,
      getColors: (theme) => ["panel-bg", "dark-purple"],
      group: otherGroup
    })
  ];
});
class FieldColorSchemeMode {
  constructor(options) {
    this.id = options.id;
    this.name = options.name;
    this.description = options.description;
    this.getNamedColors = options.getColors;
    this.isContinuous = options.isContinuous;
    this.isByValue = options.isByValue;
    this.useSeriesName = options.useSeriesName;
    this.interpolator = options.interpolator;
    this.group = options.group;
  }
  getColors(theme) {
    if (!this.getNamedColors) {
      if (!this.interpolator) {
        return [];
      }
      this.getNamedColors = () => new Array(9).fill(0).map((_, i) => this.getInterpolator()(i / 8));
    }
    if (this.colorCache && this.colorCacheTheme === theme) {
      return this.colorCache;
    }
    this.colorCache = this.getNamedColors(theme).map(theme.visualization.getColorByName);
    this.colorCacheTheme = theme;
    return this.colorCache;
  }
  getInterpolator() {
    if (!this.interpolator) {
      this.interpolator = d3Interpolate.interpolateRgbBasis(this.colorCache);
    }
    return this.interpolator;
  }
  getCalculator(field, theme) {
    const colors = this.getColors(theme);
    if (this.isByValue) {
      if (this.isContinuous) {
        return (_, percent, _threshold) => {
          return this.getInterpolator()(percent);
        };
      } else {
        return (_, percent, _threshold) => {
          return colors[percent * (colors.length - 1)];
        };
      }
    } else if (this.useSeriesName) {
      return (_, _percent, _threshold) => {
        var _a, _b;
        return getColorByStringHash(colors, (_b = (_a = field.state) == null ? void 0 : _a.displayName) != null ? _b : field.name);
      };
    } else {
      return (_, _percent, _threshold) => {
        var _a, _b;
        const seriesIndex = (_b = (_a = field.state) == null ? void 0 : _a.seriesIndex) != null ? _b : 0;
        return colors[seriesIndex % colors.length];
      };
    }
  }
}
function getFieldColorModeForField(field) {
  var _a, _b;
  return (_b = fieldColorModeRegistry.getIfExists((_a = field.config.color) == null ? void 0 : _a.mode)) != null ? _b : fieldColorModeRegistry.get(fieldColor.FieldColorModeId.Thresholds);
}
function getFieldColorMode(mode) {
  var _a;
  return (_a = fieldColorModeRegistry.getIfExists(mode)) != null ? _a : fieldColorModeRegistry.get(fieldColor.FieldColorModeId.Thresholds);
}
function getFieldSeriesColor(field, theme) {
  var _a, _b, _c;
  const mode = getFieldColorModeForField(field);
  if (!mode.isByValue) {
    return {
      color: mode.getCalculator(field, theme)(0, 0),
      threshold: thresholds.fallBackThreshold,
      percent: 1
    };
  }
  const scale$1 = scale.getScaleCalculator(field, theme);
  const stat = (_b = (_a = field.config.color) == null ? void 0 : _a.seriesBy) != null ? _b : "last";
  const calcs = fieldReducer.reduceField({ field, reducers: [stat] });
  const value = (_c = calcs[stat]) != null ? _c : 0;
  return scale$1(value);
}
function getColorByStringHash(colors, string) {
  return colors[Math.abs(stringHash__default.default(string)) % colors.length];
}
function getFixedColor(field, theme) {
  return () => {
    var _a, _b;
    return theme.visualization.getColorByName((_b = (_a = field.config.color) == null ? void 0 : _a.fixedColor) != null ? _b : fieldColor.FALLBACK_COLOR);
  };
}
function getShadedColor(field, theme) {
  return () => {
    var _a, _b, _c, _d;
    const baseColorString = theme.visualization.getColorByName(
      (_b = (_a = field.config.color) == null ? void 0 : _a.fixedColor) != null ? _b : fieldColor.FALLBACK_COLOR
    );
    const colors = [
      baseColorString
      // start with base color
    ];
    const shadesCount = 6;
    const maxHueSpin = 10;
    const maxDarken = 35;
    const maxBrighten = 35;
    for (let i = 1; i < shadesCount; i++) {
      colors.push(
        tinycolor__default.default(baseColorString).spin(i / shadesCount * maxHueSpin).brighten(i / shadesCount * maxDarken).toHexString()
      );
      colors.push(
        tinycolor__default.default(baseColorString).spin(-(i / shadesCount) * maxHueSpin).darken(i / shadesCount * maxBrighten).toHexString()
      );
    }
    const seriesIndex = (_d = (_c = field.state) == null ? void 0 : _c.seriesIndex) != null ? _d : 0;
    return colors[seriesIndex % colors.length];
  };
}

exports.FieldColorSchemeMode = FieldColorSchemeMode;
exports.fieldColorModeRegistry = fieldColorModeRegistry;
exports.getColorByStringHash = getColorByStringHash;
exports.getFieldColorMode = getFieldColorMode;
exports.getFieldColorModeForField = getFieldColorModeForField;
exports.getFieldSeriesColor = getFieldSeriesColor;
//# sourceMappingURL=fieldColor.cjs.map
