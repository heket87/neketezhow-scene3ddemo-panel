'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var fieldColor = require('../types/fieldColor.cjs');
var thresholds = require('../types/thresholds.cjs');

"use strict";
const fallBackThreshold = { value: 0, color: fieldColor.FALLBACK_COLOR };
function getActiveThreshold(value, thresholds) {
  if (!thresholds || thresholds.length === 0) {
    return fallBackThreshold;
  }
  let active = thresholds[0];
  for (const threshold of thresholds) {
    if (value >= threshold.value) {
      active = threshold;
    } else {
      break;
    }
  }
  return active;
}
function getActiveThresholdForValue(field, value, percent) {
  const { thresholds: thresholds$1 } = field.config;
  if ((thresholds$1 == null ? void 0 : thresholds$1.mode) === thresholds.ThresholdsMode.Percentage) {
    return getActiveThreshold(percent * 100, thresholds$1 == null ? void 0 : thresholds$1.steps);
  }
  return getActiveThreshold(value, thresholds$1 == null ? void 0 : thresholds$1.steps);
}
function sortThresholds(thresholds) {
  return thresholds.sort((t1, t2) => t1.value - t2.value);
}

exports.fallBackThreshold = fallBackThreshold;
exports.getActiveThreshold = getActiveThreshold;
exports.getActiveThresholdForValue = getActiveThresholdForValue;
exports.sortThresholds = sortThresholds;
//# sourceMappingURL=thresholds.cjs.map
