'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var dataFrame = require('../types/dataFrame.cjs');
var processDataFrame = require('./processDataFrame.cjs');

"use strict";
const MAX_TIME_COMPARISONS = 100;
function isTimeSeriesFrame(frame) {
  if (frame.fields.length < 2) {
    return false;
  }
  const numberField = frame.fields.find((field) => field.type === dataFrame.FieldType.number);
  let timeFieldFound = false;
  for (const field of frame.fields) {
    if (isTimeSeriesField(field)) {
      timeFieldFound = true;
      break;
    }
  }
  return timeFieldFound && numberField !== void 0;
}
function isTimeSeriesFrames(data) {
  return !data.find((frame) => !isTimeSeriesFrame(frame));
}
function isTimeSeriesField(field) {
  if (field.type !== dataFrame.FieldType.time) {
    return false;
  }
  let greatestTime = null;
  let testWindow = field.values.length > MAX_TIME_COMPARISONS ? MAX_TIME_COMPARISONS : field.values.length;
  for (let i = 0; i < testWindow; i++) {
    const time = field.values[i];
    if (greatestTime === null || time !== null && time > greatestTime) {
      greatestTime = time;
    } else {
      return false;
    }
  }
  return true;
}
function anySeriesWithTimeField(data) {
  for (let i = 0; i < data.length; i++) {
    const timeField = processDataFrame.getTimeField(data[i]);
    if (timeField.timeField !== void 0 && timeField.timeIndex !== void 0) {
      return true;
    }
  }
  return false;
}
function hasTimeField(data) {
  return data.fields.some((field) => field.type === dataFrame.FieldType.time);
}
function getRowUniqueId(dataFrame, rowIndex) {
  var _a;
  if (((_a = dataFrame.meta) == null ? void 0 : _a.uniqueRowIdFields) === void 0) {
    return void 0;
  }
  return dataFrame.meta.uniqueRowIdFields.map((fieldIndex) => dataFrame.fields[fieldIndex].values[rowIndex]).join("-");
}
function addRow(dataFrame, row) {
  if (row instanceof Array) {
    for (let i = 0; i < row.length; i++) {
      dataFrame.fields[i].values.push(row[i]);
    }
  } else {
    for (const field of dataFrame.fields) {
      field.values.push(row[field.name]);
    }
  }
  try {
    dataFrame.length++;
  } catch (e) {
  }
}
function alignTimeRangeCompareData(series, diff, theme) {
  series.fields.forEach((field) => {
    var _a, _b, _c, _d;
    if (field.type === dataFrame.FieldType.time) {
      field.values = field.values.map((v) => {
        return diff < 0 ? v - diff : v + diff;
      });
    }
    field.config = {
      ...(_a = field.config) != null ? _a : {},
      custom: {
        ...(_c = (_b = field.config) == null ? void 0 : _b.custom) != null ? _c : {},
        timeCompare: {
          diffMs: diff,
          isTimeShiftQuery: true
        }
      }
    };
    if (field.type === dataFrame.FieldType.number || field.type === dataFrame.FieldType.boolean || field.type === dataFrame.FieldType.enum) {
      field.config.custom = {
        ...(_d = field.config.custom) != null ? _d : {},
        lineStyle: {
          fill: "dash",
          dash: [1, 5, 4, 5]
        }
      };
    }
  });
}
function shouldAlignTimeCompare(compareFrame, allFrames, timeRange) {
  const compareRefId = compareFrame.refId;
  if (!compareRefId || !compareRefId.endsWith("-compare")) {
    return false;
  }
  const originalRefId = compareRefId.replace("-compare", "");
  const originalFrame = allFrames.find(
    (frame) => {
      var _a, _b;
      return frame.refId === originalRefId && !((_b = (_a = frame.meta) == null ? void 0 : _a.timeCompare) == null ? void 0 : _b.isTimeShiftQuery);
    }
  );
  if (!originalFrame) {
    return false;
  }
  const compareTimeField = compareFrame.fields.find((field) => field.type === dataFrame.FieldType.time);
  const originalTimeField = originalFrame.fields.find((field) => field.type === dataFrame.FieldType.time);
  if (!(compareTimeField == null ? void 0 : compareTimeField.values.length) || !(originalTimeField == null ? void 0 : originalTimeField.values.length)) {
    return false;
  }
  const compareFirstTime = compareTimeField.values.find((value) => value != null);
  const originalFirstTime = originalTimeField.values.find((value) => value != null);
  if (compareFirstTime == null || originalFirstTime == null) {
    return false;
  }
  return compareFirstTime < timeRange.from.valueOf();
}

exports.addRow = addRow;
exports.alignTimeRangeCompareData = alignTimeRangeCompareData;
exports.anySeriesWithTimeField = anySeriesWithTimeField;
exports.getRowUniqueId = getRowUniqueId;
exports.hasTimeField = hasTimeField;
exports.isTimeSeriesField = isTimeSeriesField;
exports.isTimeSeriesFrame = isTimeSeriesFrame;
exports.isTimeSeriesFrames = isTimeSeriesFrames;
exports.shouldAlignTimeCompare = shouldAlignTimeCompare;
//# sourceMappingURL=utils.cjs.map
