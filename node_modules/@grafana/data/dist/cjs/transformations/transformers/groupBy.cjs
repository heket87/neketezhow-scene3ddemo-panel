'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var operators = require('rxjs/operators');
var fieldState = require('../../field/fieldState.cjs');
var transformations = require('../../types/transformations.cjs');
var fieldReducer = require('../fieldReducer.cjs');
var matchers = require('../matchers.cjs');
var ids = require('../matchers/ids.cjs');
var ids$1 = require('./ids.cjs');
var utils = require('./utils.cjs');

"use strict";
const MINIMUM_FIELDS_REQUIRED = 1;
var GroupByOperationID = /* @__PURE__ */ ((GroupByOperationID2) => {
  GroupByOperationID2["aggregate"] = "aggregate";
  GroupByOperationID2["groupBy"] = "groupby";
  return GroupByOperationID2;
})(GroupByOperationID || {});
const groupByTransformer = {
  id: ids$1.DataTransformerID.groupBy,
  name: "Group by",
  description: "Group the data by a field values then process calculations for each group.",
  defaultOptions: {
    fields: {}
  },
  isApplicable: (data) => {
    const maxFields = utils.findMaxFields(data);
    return maxFields >= MINIMUM_FIELDS_REQUIRED ? transformations.TransformationApplicabilityLevels.Applicable : transformations.TransformationApplicabilityLevels.NotApplicable;
  },
  isApplicableDescription: (data) => {
    const maxFields = utils.findMaxFields(data);
    return `The Group by transformation requires a series with at least ${MINIMUM_FIELDS_REQUIRED} fields to work. The maximum number of fields found on a series is ${maxFields}`;
  },
  /**
   * Return a modified copy of the series. If the transform is not or should not
   * be applied, just return the input series
   */
  operator: (options) => (source) => source.pipe(
    operators.map((data) => {
      var _a;
      const groupByFieldNames = [];
      for (const [k, v] of Object.entries(options.fields)) {
        if (v.operation === "groupby" /* groupBy */) {
          groupByFieldNames.push(k);
        }
      }
      if (groupByFieldNames.length === 0) {
        return data;
      }
      const matcher = matchers.getFieldMatcher({
        id: ids.FieldMatcherID.byNames,
        options: { names: groupByFieldNames }
      });
      const processed = [];
      for (const frame of data) {
        const groupByFields = frame.fields.filter((field) => matcher(field, frame, data));
        if (groupByFields.length === 0) {
          continue;
        }
        const valuesByGroupKey = groupValuesByKey(frame, groupByFields);
        const fields = createGroupedFields(groupByFields, valuesByGroupKey);
        for (const field of frame.fields) {
          if (!shouldCalculateField(field, options)) {
            continue;
          }
          const fieldName = fieldState.getFieldDisplayName(field);
          const aggregations = options.fields[fieldName].aggregations;
          const valuesByAggregation = {};
          valuesByGroupKey.forEach((value) => {
            const fieldWithValuesForGroup = value[fieldName];
            const results = fieldReducer.reduceField({
              field: fieldWithValuesForGroup,
              reducers: aggregations
            });
            for (const aggregation of aggregations) {
              if (!Array.isArray(valuesByAggregation[aggregation])) {
                valuesByAggregation[aggregation] = [];
              }
              valuesByAggregation[aggregation].push(results[aggregation]);
            }
          });
          for (const aggregation of aggregations) {
            const aggregationField = {
              name: `${fieldName} (${aggregation})`,
              values: (_a = valuesByAggregation[aggregation]) != null ? _a : [],
              type: fieldReducer.getFieldTypeForReducer(aggregation, field.type),
              config: {}
            };
            fields.push(aggregationField);
          }
        }
        processed.push({
          refId: frame.refId,
          fields,
          length: valuesByGroupKey.size
        });
      }
      return processed;
    })
  )
};
const shouldCalculateField = (field, options) => {
  var _a;
  const fieldName = fieldState.getFieldDisplayName(field);
  const { operation, aggregations = [] } = (_a = options.fields[fieldName]) != null ? _a : {};
  if (!Array.isArray(aggregations)) {
    return false;
  } else if (operation === "aggregate" /* aggregate */) {
    return aggregations.length > 0;
  } else if (operation === "groupby" /* groupBy */) {
    return aggregations.length === 1 && aggregations[0] === fieldReducer.ReducerID.count;
  } else {
    return false;
  }
};
function groupValuesByKey(frame, groupByFields) {
  var _a;
  const valuesByGroupKey = /* @__PURE__ */ new Map();
  for (let rowIndex = 0; rowIndex < frame.length; rowIndex++) {
    const groupKey = String(groupByFields.map((field) => field.values[rowIndex]));
    const valuesByField = (_a = valuesByGroupKey.get(groupKey)) != null ? _a : {};
    if (!valuesByGroupKey.has(groupKey)) {
      valuesByGroupKey.set(groupKey, valuesByField);
    }
    for (let field of frame.fields) {
      const fieldName = fieldState.getFieldDisplayName(field);
      if (!valuesByField[fieldName]) {
        valuesByField[fieldName] = {
          name: fieldName,
          type: field.type,
          config: { ...field.config },
          values: []
        };
      }
      valuesByField[fieldName].values.push(field.values[rowIndex]);
    }
  }
  return valuesByGroupKey;
}
function createGroupedFields(groupByFields, valuesByGroupKey) {
  const fields = [];
  for (const field of groupByFields) {
    const values = [];
    const fieldName = fieldState.getFieldDisplayName(field);
    valuesByGroupKey.forEach((value) => {
      values.push(value[fieldName].values[0]);
    });
    fields.push({
      name: field.name,
      type: field.type,
      config: {
        ...field.config
      },
      values
    });
  }
  return fields;
}

exports.GroupByOperationID = GroupByOperationID;
exports.createGroupedFields = createGroupedFields;
exports.groupByTransformer = groupByTransformer;
exports.groupValuesByKey = groupValuesByKey;
exports.shouldCalculateField = shouldCalculateField;
//# sourceMappingURL=groupBy.cjs.map
