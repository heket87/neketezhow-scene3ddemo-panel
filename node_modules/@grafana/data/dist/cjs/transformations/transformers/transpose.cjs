'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var operators = require('rxjs/operators');
var fieldState = require('../../field/fieldState.cjs');
var dataFrame = require('../../types/dataFrame.cjs');
var transformations = require('../../types/transformations.cjs');
var ids = require('./ids.cjs');
var utils = require('./utils.cjs');

"use strict";
const transposeTransformer = {
  id: ids.DataTransformerID.transpose,
  name: "Transpose",
  description: "Transpose the data frame",
  defaultOptions: {},
  operator: (options) => (source) => source.pipe(
    operators.map((data) => {
      if (data.length === 0) {
        return data;
      }
      return transposeDataFrame(options, data);
    })
  )
};
function transposeDataFrame(options, data) {
  var _a;
  fieldState.cacheFieldDisplayNames(data);
  const emptyValue = (_a = options.emptyValue) != null ? _a : transformations.SpecialValue.Empty;
  return data.map((frame) => {
    const firstField = frame.fields[0];
    const firstName = !options.firstFieldName ? "Field" : options.firstFieldName;
    const restName = !options.restFieldsName ? "Value" : options.restFieldsName;
    const useFirstFieldAsHeaders = firstField.type === dataFrame.FieldType.string || firstField.type === dataFrame.FieldType.time || firstField.type === dataFrame.FieldType.enum;
    const headers = useFirstFieldAsHeaders ? [firstName, ...fieldValuesAsStrings(firstField, firstField.values, emptyValue)] : [firstName, ...firstField.values.map((_, i) => restName)];
    const rows = useFirstFieldAsHeaders ? frame.fields.map((field) => {
      var _a2, _b;
      return (_b = (_a2 = field.state) == null ? void 0 : _a2.displayName) != null ? _b : field.name;
    }).slice(1) : frame.fields.map((field) => {
      var _a2, _b;
      return (_b = (_a2 = field.state) == null ? void 0 : _a2.displayName) != null ? _b : field.name;
    });
    const fieldType = determineFieldType(
      useFirstFieldAsHeaders ? frame.fields.map((field) => field.type).slice(1) : frame.fields.map((field) => field.type)
    );
    const newFields = headers.map((fieldName, index) => {
      if (index === 0) {
        return {
          name: firstName,
          type: dataFrame.FieldType.string,
          config: {},
          values: rows
        };
      }
      const values = frame.fields.map((field) => {
        if (fieldType === dataFrame.FieldType.string) {
          return fieldValuesAsStrings(field, [field.values[index - 1]], emptyValue)[0];
        }
        return field.values[index - 1];
      });
      const labelName = useFirstFieldAsHeaders ? firstField.name : "row";
      const labelValue = useFirstFieldAsHeaders ? fieldName : index;
      return {
        name: useFirstFieldAsHeaders ? restName : fieldName,
        labels: {
          [labelName]: labelValue
        },
        type: fieldType,
        config: {},
        values: useFirstFieldAsHeaders ? values.slice(1) : values
      };
    });
    return {
      ...frame,
      fields: newFields,
      length: Math.max(...newFields.map((field) => field.values.length)),
      refId: `${ids.DataTransformerID.transpose}-${frame.refId}`
    };
  });
}
function determineFieldType(fieldTypes) {
  const uniqueFieldTypes = new Set(fieldTypes);
  return uniqueFieldTypes.size === 1 ? [...uniqueFieldTypes][0] : dataFrame.FieldType.string;
}
function fieldValuesAsStrings(field, values, emptyValue) {
  switch (field.type) {
    case dataFrame.FieldType.time:
    case dataFrame.FieldType.number:
    case dataFrame.FieldType.boolean:
    case dataFrame.FieldType.string:
      return values.map((v) => v != null ? `${v}` : utils.getSpecialValue(emptyValue));
    case dataFrame.FieldType.enum:
      return values.map((v) => {
        var _a;
        return (_a = field.config.type.enum.text[v]) != null ? _a : utils.getSpecialValue(emptyValue);
      });
    default:
      return values.map((v) => v != null ? JSON.stringify(v) : utils.getSpecialValue(emptyValue));
  }
}

exports.transposeTransformer = transposeTransformer;
//# sourceMappingURL=transpose.cjs.map
