'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var operators = require('rxjs/operators');
var fieldState = require('../../field/fieldState.cjs');
var transformations = require('../../types/transformations.cjs');
var convertFieldType = require('./convertFieldType.cjs');
var ids = require('./ids.cjs');

"use strict";
const formatTimeTransformer = {
  id: ids.DataTransformerID.formatTime,
  name: "Format time",
  description: "Set the output format of a time field",
  defaultOptions: { timeField: "", outputFormat: "", useTimezone: true },
  isApplicable: (data) => {
    for (const frame of data) {
      for (const field of frame.fields) {
        if (field.type === "time") {
          return transformations.TransformationApplicabilityLevels.Applicable;
        }
      }
    }
    return transformations.TransformationApplicabilityLevels.NotApplicable;
  },
  isApplicableDescription: "The Format time transformation requires a time field to work. No time field could be found.",
  operator: (options, ctx) => (source) => source.pipe(
    operators.map((data) => {
      return applyFormatTime(options, data, ctx);
    })
  )
};
const applyFormatTime = ({ timeField, outputFormat, timezone }, data, ctx) => {
  var _a;
  if (!Array.isArray(data) || data.length === 0) {
    return data;
  }
  fieldState.cacheFieldDisplayNames(data);
  outputFormat = (_a = ctx == null ? void 0 : ctx.interpolate(outputFormat)) != null ? _a : outputFormat;
  return data.map((frame) => ({
    ...frame,
    fields: frame.fields.map((field) => {
      var _a2;
      if (((_a2 = field.state) == null ? void 0 : _a2.displayName) === timeField) {
        field = convertFieldType.fieldToStringField(field, outputFormat, { timeZone: timezone });
      }
      return field;
    })
  }));
};

exports.applyFormatTime = applyFormatTime;
exports.formatTimeTransformer = formatTimeTransformer;
//# sourceMappingURL=formatTime.cjs.map
