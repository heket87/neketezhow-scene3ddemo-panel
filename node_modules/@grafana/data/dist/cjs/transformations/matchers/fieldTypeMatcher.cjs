'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var dataFrame = require('../../types/dataFrame.cjs');
var ids = require('./ids.cjs');

"use strict";
const fieldTypeMatcher = {
  id: ids.FieldMatcherID.byType,
  name: "Field Type",
  description: "match based on the field type",
  defaultOptions: dataFrame.FieldType.number,
  get: (type) => {
    return (field, frame, allFrames) => {
      return type === field.type;
    };
  },
  getOptionsDisplayText: (type) => {
    return `Field type: ${type}`;
  }
};
const fieldTypesMatcher = {
  id: ids.FieldMatcherID.byTypes,
  name: "Field Type",
  description: "match based on the field types",
  defaultOptions: /* @__PURE__ */ new Set(),
  get: (types) => {
    return (field, frame, allFrames) => {
      return types.has(field.type);
    };
  },
  getOptionsDisplayText: (types) => {
    return `Field types: ${[...types].join(" | ")}`;
  }
};
const numericMatcher = {
  id: ids.FieldMatcherID.numeric,
  name: "Numeric Fields",
  description: "Fields with type number",
  get: () => {
    return fieldTypeMatcher.get(dataFrame.FieldType.number);
  },
  getOptionsDisplayText: () => {
    return "Numeric Fields";
  }
};
const timeMatcher = {
  id: ids.FieldMatcherID.time,
  name: "Time Fields",
  description: "Fields with type time",
  get: () => {
    return fieldTypeMatcher.get(dataFrame.FieldType.time);
  },
  getOptionsDisplayText: () => {
    return "Time Fields";
  }
};
function getFieldTypeMatchers() {
  return [fieldTypeMatcher, fieldTypesMatcher, numericMatcher, timeMatcher];
}

exports.getFieldTypeMatchers = getFieldTypeMatchers;
//# sourceMappingURL=fieldTypeMatcher.cjs.map
