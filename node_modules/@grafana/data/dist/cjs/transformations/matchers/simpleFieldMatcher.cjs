'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var dataFrame = require('../../types/dataFrame.cjs');
var ids = require('./ids.cjs');

"use strict";
const firstFieldMatcher = {
  id: ids.FieldMatcherID.first,
  name: "First Field",
  description: "The first field in the frame",
  get: (type) => {
    return (field, frame, allFrames) => {
      return field === frame.fields[0];
    };
  },
  getOptionsDisplayText: () => {
    return `First field`;
  }
};
const firstTimeFieldMatcher = {
  id: ids.FieldMatcherID.firstTimeField,
  name: "First time field",
  description: "The first field of type time in a frame",
  get: (type) => {
    return (field, frame, allFrames) => {
      return field.type === dataFrame.FieldType.time && field === frame.fields.find((f) => f.type === dataFrame.FieldType.time);
    };
  },
  getOptionsDisplayText: () => {
    return `First time field`;
  }
};
function getSimpleFieldMatchers() {
  return [firstFieldMatcher, firstTimeFieldMatcher];
}

exports.getSimpleFieldMatchers = getSimpleFieldMatchers;
//# sourceMappingURL=simpleFieldMatcher.cjs.map
