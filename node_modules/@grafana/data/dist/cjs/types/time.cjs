'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var schema = require('@grafana/schema');
var moment_wrapper = require('../datetime/moment_wrapper.cjs');

"use strict";
const DefaultTimeZone = schema.defaultTimeZone;
const TIME_FORMAT = "YYYY-MM-DD HH:mm:ss";
function getDefaultTimeRange() {
  const now = moment_wrapper.dateTime();
  return {
    from: moment_wrapper.dateTime(now).subtract(6, "hour"),
    to: now,
    raw: { from: "now-6h", to: "now" }
  };
}
function getDefaultRelativeTimeRange() {
  return {
    from: 600,
    to: 0
  };
}
function makeTimeRange(from, to) {
  const fromDateTime = typeof from === "string" ? moment_wrapper.dateTime(from) : from;
  const toDateTime = typeof to === "string" ? moment_wrapper.dateTime(to) : to;
  return {
    from: fromDateTime,
    to: toDateTime,
    raw: {
      from: fromDateTime,
      to: toDateTime
    }
  };
}

exports.DefaultTimeZone = DefaultTimeZone;
exports.TIME_FORMAT = TIME_FORMAT;
exports.getDefaultRelativeTimeRange = getDefaultRelativeTimeRange;
exports.getDefaultTimeRange = getDefaultTimeRange;
exports.makeTimeRange = makeTimeRange;
//# sourceMappingURL=time.cjs.map
