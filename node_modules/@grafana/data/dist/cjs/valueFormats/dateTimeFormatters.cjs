'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var formats = require('../datetime/formats.cjs');
var formatter = require('../datetime/formatter.cjs');
var moment_wrapper = require('../datetime/moment_wrapper.cjs');
var valueFormats = require('./valueFormats.cjs');

"use strict";
var Interval = /* @__PURE__ */ ((Interval2) => {
  Interval2["Year"] = "year";
  Interval2["Month"] = "month";
  Interval2["Week"] = "week";
  Interval2["Day"] = "day";
  Interval2["Hour"] = "hour";
  Interval2["Minute"] = "minute";
  Interval2["Second"] = "second";
  Interval2["Millisecond"] = "millisecond";
  return Interval2;
})(Interval || {});
const UNITS = [
  "year" /* Year */,
  "month" /* Month */,
  "week" /* Week */,
  "day" /* Day */,
  "hour" /* Hour */,
  "minute" /* Minute */,
  "second" /* Second */,
  "millisecond" /* Millisecond */
];
const INTERVALS_IN_SECONDS = {
  ["year" /* Year */]: 31536e3,
  ["month" /* Month */]: 2592e3,
  ["week" /* Week */]: 604800,
  ["day" /* Day */]: 86400,
  ["hour" /* Hour */]: 3600,
  ["minute" /* Minute */]: 60,
  ["second" /* Second */]: 1,
  ["millisecond" /* Millisecond */]: 1e-3
};
function toNanoSeconds(size, decimals) {
  if (size === null) {
    return { text: "" };
  }
  if (Math.abs(size) < 1e3) {
    return { text: valueFormats.toFixed(size, decimals), suffix: " ns" };
  } else if (Math.abs(size) < 1e6) {
    return valueFormats.toFixedScaled(size / 1e3, decimals, " \xB5s");
  } else if (Math.abs(size) < 1e9) {
    return valueFormats.toFixedScaled(size / 1e6, decimals, " ms");
  } else if (Math.abs(size) < 6e10) {
    return valueFormats.toFixedScaled(size / 1e9, decimals, " s");
  } else if (Math.abs(size) < 36e11) {
    return valueFormats.toFixedScaled(size / 6e10, decimals, " min");
  } else if (Math.abs(size) < 864e11) {
    return valueFormats.toFixedScaled(size / 36e11, decimals, " hour");
  } else {
    return valueFormats.toFixedScaled(size / 864e11, decimals, " day");
  }
}
function toMicroSeconds(size, decimals) {
  if (size === null) {
    return { text: "" };
  }
  if (Math.abs(size) < 1e3) {
    return { text: valueFormats.toFixed(size, decimals), suffix: " \xB5s" };
  } else if (Math.abs(size) < 1e6) {
    return valueFormats.toFixedScaled(size / 1e3, decimals, " ms");
  } else {
    return valueFormats.toFixedScaled(size / 1e6, decimals, " s");
  }
}
function toMilliSeconds(size, decimals, scaledDecimals) {
  if (size === null) {
    return { text: "" };
  }
  if (Math.abs(size) < 1e3) {
    return { text: valueFormats.toFixed(size, decimals), suffix: " ms" };
  } else if (Math.abs(size) < 6e4) {
    return valueFormats.toFixedScaled(size / 1e3, decimals, " s");
  } else if (Math.abs(size) < 36e5) {
    return valueFormats.toFixedScaled(size / 6e4, decimals, " min");
  } else if (Math.abs(size) < 864e5) {
    return valueFormats.toFixedScaled(size / 36e5, decimals, " hour");
  } else if (Math.abs(size) < 31536e6) {
    return valueFormats.toFixedScaled(size / 864e5, decimals, " day");
  }
  return valueFormats.toFixedScaled(size / 31536e6, decimals, " year");
}
function toSeconds(size, decimals) {
  if (size === null) {
    return { text: "" };
  }
  if (size === 0) {
    return { text: "0", suffix: " s" };
  }
  if (Math.abs(size) < 1e-6) {
    return valueFormats.toFixedScaled(size * 1e9, decimals, " ns");
  }
  if (Math.abs(size) < 1e-3) {
    return valueFormats.toFixedScaled(size * 1e6, decimals, " \xB5s");
  }
  if (Math.abs(size) < 1) {
    return valueFormats.toFixedScaled(size * 1e3, decimals, " ms");
  }
  if (Math.abs(size) < 60) {
    return { text: valueFormats.toFixed(size, decimals), suffix: " s" };
  } else if (Math.abs(size) < 3600) {
    return valueFormats.toFixedScaled(size / 60, decimals, " min");
  } else if (Math.abs(size) < 86400) {
    return valueFormats.toFixedScaled(size / 3600, decimals, " hour");
  } else if (Math.abs(size) < 604800) {
    return valueFormats.toFixedScaled(size / 86400, decimals, " day");
  } else if (Math.abs(size) < 31536e3) {
    return valueFormats.toFixedScaled(size / 604800, decimals, " week");
  }
  return valueFormats.toFixedScaled(size / 31556900, decimals, " year");
}
function toMinutes(size, decimals) {
  if (size === null) {
    return { text: "" };
  }
  if (Math.abs(size) < 60) {
    return { text: valueFormats.toFixed(size, decimals), suffix: " min" };
  } else if (Math.abs(size) < 1440) {
    return valueFormats.toFixedScaled(size / 60, decimals, " hour");
  } else if (Math.abs(size) < 10080) {
    return valueFormats.toFixedScaled(size / 1440, decimals, " day");
  } else if (Math.abs(size) < 604800) {
    return valueFormats.toFixedScaled(size / 10080, decimals, " week");
  } else {
    return valueFormats.toFixedScaled(size / 525948, decimals, " year");
  }
}
function toHours(size, decimals) {
  if (size === null) {
    return { text: "" };
  }
  if (Math.abs(size) < 24) {
    return { text: valueFormats.toFixed(size, decimals), suffix: " hour" };
  } else if (Math.abs(size) < 168) {
    return valueFormats.toFixedScaled(size / 24, decimals, " day");
  } else if (Math.abs(size) < 8760) {
    return valueFormats.toFixedScaled(size / 168, decimals, " week");
  } else {
    return valueFormats.toFixedScaled(size / 8760, decimals, " year");
  }
}
function toDays(size, decimals) {
  if (size === null) {
    return { text: "" };
  }
  if (Math.abs(size) < 7) {
    return valueFormats.toFixedScaled(size, decimals, " day");
  } else if (Math.abs(size) < 365) {
    return valueFormats.toFixedScaled(size / 7, decimals, " week");
  } else {
    return valueFormats.toFixedScaled(size / 365, decimals, " year");
  }
}
function toDuration(size, decimals, timeScale) {
  if (size === null) {
    return { text: "" };
  }
  if (size === 0) {
    return { text: "0", suffix: " " + timeScale + "s" };
  }
  if (size < 0) {
    const v = toDuration(-size, decimals, timeScale);
    if (!v.suffix) {
      v.suffix = "";
    }
    v.suffix += " ago";
    return v;
  }
  size *= INTERVALS_IN_SECONDS[timeScale] * 1e3;
  const strings = [];
  let decrementDecimals = false;
  let decimalsCount = 0;
  if (decimals !== null && decimals !== void 0) {
    decimalsCount = decimals;
  }
  for (let i = 0; i < UNITS.length && decimalsCount >= 0; i++) {
    const interval = INTERVALS_IN_SECONDS[UNITS[i]] * 1e3;
    const value = size / interval;
    if (value >= 1 || decrementDecimals) {
      decrementDecimals = true;
      const floor = Math.floor(value);
      const unit = UNITS[i] + (floor !== 1 ? "s" : "");
      strings.push(floor + " " + unit);
      size = size % interval;
      decimalsCount--;
    }
  }
  return { text: strings.join(", ") };
}
function toClock(size, decimals) {
  if (size === null) {
    return { text: "" };
  }
  if (size < 1e3) {
    return {
      text: moment_wrapper.toUtc(size).format("SSS\\m\\s")
    };
  }
  if (size < 6e4) {
    let format2 = "ss\\s:SSS\\m\\s";
    if (decimals === 0) {
      format2 = "ss\\s";
    }
    return { text: moment_wrapper.toUtc(size).format(format2) };
  }
  if (size < 36e5) {
    let format2 = "mm\\m:ss\\s:SSS\\m\\s";
    if (decimals === 0) {
      format2 = "mm\\m";
    } else if (decimals === 1) {
      format2 = "mm\\m:ss\\s";
    }
    return { text: moment_wrapper.toUtc(size).format(format2) };
  }
  let format = "mm\\m:ss\\s:SSS\\m\\s";
  const hours = `${Math.floor(moment_wrapper.toDuration(size, "milliseconds").asHours())}h`;
  if (decimals === 0) {
    format = "";
  } else if (decimals === 1) {
    format = "mm\\m";
  } else if (decimals === 2) {
    format = "mm\\m:ss\\s";
  }
  const text = format ? `${hours}:${moment_wrapper.toUtc(size).format(format)}` : hours;
  return { text };
}
function toDurationInMilliseconds(size, decimals) {
  return toDuration(size, decimals, "millisecond" /* Millisecond */);
}
function toDurationInSeconds(size, decimals) {
  return toDuration(size, decimals, "second" /* Second */);
}
function toDurationInHoursMinutesSeconds(size) {
  if (size < 0) {
    const v = toDurationInHoursMinutesSeconds(-size);
    if (!v.suffix) {
      v.suffix = "";
    }
    v.suffix += " ago";
    return v;
  }
  const strings = [];
  const numHours = Math.floor(size / 3600);
  const numMinutes = Math.floor(size % 3600 / 60);
  const numSeconds = Math.floor(size % 3600 % 60);
  numHours > 9 ? strings.push("" + numHours) : strings.push("0" + numHours);
  numMinutes > 9 ? strings.push("" + numMinutes) : strings.push("0" + numMinutes);
  numSeconds > 9 ? strings.push("" + numSeconds) : strings.push("0" + numSeconds);
  return { text: strings.join(":") };
}
function toDurationInDaysHoursMinutesSeconds(size) {
  if (size < 0) {
    const v = toDurationInDaysHoursMinutesSeconds(-size);
    if (!v.suffix) {
      v.suffix = "";
    }
    v.suffix += " ago";
    return v;
  }
  let dayString = "";
  const numDays = Math.floor(size / (24 * 3600));
  if (numDays > 0) {
    dayString = numDays + " d ";
  }
  const hmsString = toDurationInHoursMinutesSeconds(size - numDays * 24 * 3600);
  return { text: dayString + hmsString.text };
}
function toTimeTicks(size, decimals) {
  return toSeconds(size / 100, decimals);
}
function toClockMilliseconds(size, decimals) {
  return toClock(size, decimals);
}
function toClockSeconds(size, decimals) {
  return toClock(size * 1e3, decimals);
}
function toDateTimeValueFormatter(pattern, todayPattern) {
  return (value, decimals, scaledDecimals, timeZone) => {
    if (todayPattern) {
      if (moment_wrapper.dateTime().isSame(value, "day")) {
        return {
          text: formatter.dateTimeFormat(value, { format: todayPattern, timeZone })
        };
      }
    }
    return { text: formatter.dateTimeFormat(value, { format: pattern, timeZone }) };
  };
}
const dateTimeAsIso = toDateTimeValueFormatter("YYYY-MM-DD HH:mm:ss");
const dateTimeAsIsoNoDateIfToday = toDateTimeValueFormatter("YYYY-MM-DD HH:mm:ss", "HH:mm:ss");
const dateTimeAsUS = toDateTimeValueFormatter("MM/DD/YYYY h:mm:ss a");
const dateTimeAsUSNoDateIfToday = toDateTimeValueFormatter("MM/DD/YYYY h:mm:ss a", "h:mm:ss a");
function getDateTimeAsLocalFormat() {
  return toDateTimeValueFormatter(
    formats.localTimeFormat({
      year: "numeric",
      month: "2-digit",
      day: "2-digit",
      hour: "2-digit",
      minute: "2-digit",
      second: "2-digit"
    })
  );
}
function getDateTimeAsLocalFormatNoDateIfToday() {
  return toDateTimeValueFormatter(
    formats.localTimeFormat({
      year: "numeric",
      month: "2-digit",
      day: "2-digit",
      hour: "2-digit",
      minute: "2-digit",
      second: "2-digit"
    }),
    formats.localTimeFormat({
      hour: "2-digit",
      minute: "2-digit",
      second: "2-digit"
    })
  );
}
function dateTimeSystemFormatter(value, decimals, scaledDecimals, timeZone, showMs) {
  return {
    text: formatter.dateTimeFormat(value, {
      format: showMs ? formats.systemDateFormats.fullDateMS : formats.systemDateFormats.fullDate,
      timeZone
    })
  };
}
function dateTimeFromNow(value, decimals, scaledDecimals, timeZone) {
  return { text: formatter.dateTimeFormatTimeAgo(value, { timeZone }) };
}

exports.Interval = Interval;
exports.dateTimeAsIso = dateTimeAsIso;
exports.dateTimeAsIsoNoDateIfToday = dateTimeAsIsoNoDateIfToday;
exports.dateTimeAsUS = dateTimeAsUS;
exports.dateTimeAsUSNoDateIfToday = dateTimeAsUSNoDateIfToday;
exports.dateTimeFromNow = dateTimeFromNow;
exports.dateTimeSystemFormatter = dateTimeSystemFormatter;
exports.getDateTimeAsLocalFormat = getDateTimeAsLocalFormat;
exports.getDateTimeAsLocalFormatNoDateIfToday = getDateTimeAsLocalFormatNoDateIfToday;
exports.toClock = toClock;
exports.toClockMilliseconds = toClockMilliseconds;
exports.toClockSeconds = toClockSeconds;
exports.toDateTimeValueFormatter = toDateTimeValueFormatter;
exports.toDays = toDays;
exports.toDuration = toDuration;
exports.toDurationInDaysHoursMinutesSeconds = toDurationInDaysHoursMinutesSeconds;
exports.toDurationInHoursMinutesSeconds = toDurationInHoursMinutesSeconds;
exports.toDurationInMilliseconds = toDurationInMilliseconds;
exports.toDurationInSeconds = toDurationInSeconds;
exports.toHours = toHours;
exports.toMicroSeconds = toMicroSeconds;
exports.toMilliSeconds = toMilliSeconds;
exports.toMinutes = toMinutes;
exports.toNanoSeconds = toNanoSeconds;
exports.toSeconds = toSeconds;
exports.toTimeTicks = toTimeTicks;
//# sourceMappingURL=dateTimeFormatters.cjs.map
