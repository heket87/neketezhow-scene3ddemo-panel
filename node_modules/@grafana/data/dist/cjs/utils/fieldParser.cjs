'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var processDataFrame = require('../dataframe/processDataFrame.cjs');
var dataFrame = require('../types/dataFrame.cjs');

"use strict";
function makeFieldParser(value, field) {
  if (!field.type) {
    if (field.name === "time" || field.name === "Time") {
      field.type = dataFrame.FieldType.time;
    } else {
      field.type = processDataFrame.guessFieldTypeFromValue(value);
    }
  }
  if (field.type === dataFrame.FieldType.number) {
    return (value2) => {
      return parseFloat(value2);
    };
  }
  if (field.type === dataFrame.FieldType.boolean) {
    return (value2) => {
      return !(value2[0] === "F" || value2[0] === "f" || value2[0] === "0");
    };
  }
  return (value2) => value2;
}

exports.makeFieldParser = makeFieldParser;
//# sourceMappingURL=fieldParser.cjs.map
