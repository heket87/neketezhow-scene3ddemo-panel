'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var jsxRuntime = require('react/jsx-runtime');
var react = require('react');
var store = require('./store.cjs');

"use strict";
const LocalStorageValueProvider = (props) => {
  const { children, storageKey, defaultValue } = props;
  const [state, setState] = react.useState({ value: store.store.getObject(props.storageKey, props.defaultValue) });
  react.useEffect(() => {
    const onStorageUpdate = (v) => {
      if (v.key === storageKey) {
        setState({ value: store.store.getObject(props.storageKey, props.defaultValue) });
      }
    };
    window.addEventListener("storage", onStorageUpdate);
    return () => {
      window.removeEventListener("storage", onStorageUpdate);
    };
  });
  const onSaveToStore = (value) => {
    try {
      store.store.setObject(storageKey, value);
    } catch (error) {
      console.error(error);
    }
    setState({ value });
  };
  const onDeleteFromStore = () => {
    try {
      store.store.delete(storageKey);
    } catch (error) {
      console.log(error);
    }
    setState({ value: defaultValue });
  };
  return /* @__PURE__ */ jsxRuntime.jsx(jsxRuntime.Fragment, { children: children(state.value, onSaveToStore, onDeleteFromStore) });
};

exports.LocalStorageValueProvider = LocalStorageValueProvider;
//# sourceMappingURL=LocalStorageValueProvider.cjs.map
