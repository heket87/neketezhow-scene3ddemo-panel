'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var z = require('zod');
var breakpoints = require('./breakpoints.cjs');
var createColors = require('./createColors.cjs');
var createComponents = require('./createComponents.cjs');
var createShadows = require('./createShadows.cjs');
var createShape = require('./createShape.cjs');
var createSpacing = require('./createSpacing.cjs');
var createTransitions = require('./createTransitions.cjs');
var createTypography = require('./createTypography.cjs');
var createV1Theme = require('./createV1Theme.cjs');
var createVisualizationColors = require('./createVisualizationColors.cjs');
var zIndex = require('./zIndex.cjs');

function _interopNamespaceCompat(e) {
  if (e && typeof e === 'object' && 'default' in e) return e;
  var n = Object.create(null);
  if (e) {
    Object.keys(e).forEach(function (k) {
      if (k !== 'default') {
        var d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: function () { return e[k]; }
        });
      }
    });
  }
  n.default = e;
  return Object.freeze(n);
}

var z__namespace = /*#__PURE__*/_interopNamespaceCompat(z);

"use strict";
const NewThemeOptionsSchema = z__namespace.object({
  name: z__namespace.string(),
  id: z__namespace.string(),
  colors: createColors.ThemeColorsInputSchema.optional(),
  spacing: createSpacing.ThemeSpacingOptionsSchema.optional(),
  shape: createShape.ThemeShapeInputSchema.optional(),
  typography: createTypography.ThemeTypographyInputSchema.optional(),
  visualization: createVisualizationColors.ThemeVisualizationColorsInputSchema.optional()
});
function createTheme(options = {}) {
  const {
    name,
    colors: colorsInput = {},
    spacing: spacingInput = {},
    shape: shapeInput = {},
    typography: typographyInput = {},
    visualization: visualizationInput = {}
  } = options;
  const colors = createColors.createColors(colorsInput);
  const breakpoints$1 = breakpoints.createBreakpoints();
  const spacing = createSpacing.createSpacing(spacingInput);
  const shape = createShape.createShape(shapeInput);
  const typography = createTypography.createTypography(colors, typographyInput);
  const shadows = createShadows.createShadows(colors);
  const transitions = createTransitions.createTransitions();
  const components = createComponents.createComponents(colors, shadows);
  const visualization = createVisualizationColors.createVisualizationColors(colors, visualizationInput);
  const theme = {
    name: name != null ? name : colors.mode === "dark" ? "Dark" : "Light",
    isDark: colors.mode === "dark",
    isLight: colors.mode === "light",
    colors,
    breakpoints: breakpoints$1,
    spacing,
    shape,
    components,
    typography,
    shadows,
    transitions,
    visualization,
    zIndex: {
      ...zIndex.zIndex
    },
    flags: {}
  };
  return {
    ...theme,
    v1: createV1Theme.createV1Theme(theme)
  };
}

exports.NewThemeOptionsSchema = NewThemeOptionsSchema;
exports.createTheme = createTheme;
//# sourceMappingURL=createTheme.cjs.map
