'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

"use strict";
function createBreakpoints() {
  const step = 5;
  const keys = ["xs", "sm", "md", "lg", "xl", "xxl"];
  const unit = "px";
  const values = {
    xs: 0,
    sm: 544,
    md: 769,
    // 1 more than regular ipad in portrait
    lg: 992,
    xl: 1200,
    xxl: 1440
  };
  function up(key) {
    const value = typeof key === "number" ? key : values[key];
    return `@media (min-width:${value}${unit})`;
  }
  function down(key) {
    const value = typeof key === "number" ? key : values[key];
    return `@media (max-width:${value - step / 100}${unit})`;
  }
  function containerUp(key, name) {
    const value = typeof key === "number" ? key : values[key];
    const query = typeof name === "string" ? `@container ${name}` : "@container";
    return `${query} (width >= ${value}${unit})`;
  }
  function containerDown(key, name) {
    const value = typeof key === "number" ? key : values[key];
    const query = typeof name === "string" ? `@container ${name}` : "@container";
    return `${query} (width < ${value}${unit})`;
  }
  return {
    values,
    up,
    down,
    keys,
    unit,
    container: {
      up: containerUp,
      down: containerDown
    }
  };
}

exports.createBreakpoints = createBreakpoints;
//# sourceMappingURL=breakpoints.cjs.map
