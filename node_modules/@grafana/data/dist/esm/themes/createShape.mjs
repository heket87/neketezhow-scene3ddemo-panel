import { z } from 'zod';

"use strict";
const ThemeShapeInputSchema = z.object({
  borderRadius: z.int().nonnegative().optional()
});
function createShape(options) {
  var _a;
  const baseBorderRadius = (_a = options.borderRadius) != null ? _a : 6;
  const radius = {
    default: `${baseBorderRadius}px`,
    md: `${baseBorderRadius}px`,
    sm: `${Math.ceil(baseBorderRadius * (2 / 3))}px`,
    // for default base becomes 4
    lg: `${Math.ceil(baseBorderRadius * (5 / 3))}px`,
    // for default base becomes 10
    pill: "9999px",
    circle: "100%"
  };
  const borderRadius = (amount) => {
    const value = (amount != null ? amount : 1) * baseBorderRadius;
    return `${value}px`;
  };
  return {
    radius,
    borderRadius
  };
}

export { ThemeShapeInputSchema, createShape };
//# sourceMappingURL=createShape.mjs.map
