import { DataFrame } from '../../types/dataFrame';
import { SpecialValue } from '../../types/transformations';
/**
 * Retrieve the maximum number of fields in a series of a dataframe.
 */
export declare function findMaxFields(data: DataFrame[]): number;
export declare function getSpecialValue(specialValue: SpecialValue): boolean | 0 | "" | null;
