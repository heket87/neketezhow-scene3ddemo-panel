interface TranslationDefinition {
    /** IETF language tag */
    code: string;
    /** The language name in its own language (e.g. "Français" for French) */
    name: string;
}
/**
 * Supported languages for translation.
 */
export declare const LANGUAGES: TranslationDefinition[];
export {};
